-module(bits).

% TODO :
%   1. For binary numbers of size `n` compute the binary values which does not
%   have consecutive 1s.
%   2. For binary numbers of size `n` compute the binary values which does not
%   have odd number of consecutive 1s.
%   3. For binary numbers of size `n` compute the binary values which does not
%   have even number of consecutive 0s or 1s.
